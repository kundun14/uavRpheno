% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pheno_functions.R
\name{extract_htp_zonal}
\alias{extract_htp_zonal}
\title{Full Remote Sensing Zonal Extraction Pipeline}
\usage{
extract_htp_zonal(
  multi_path = NULL,
  dsm_path = NULL,
  border_path = NULL,
  multiborder_path = NULL,
  dtm_file = NULL,
  save_rasters = FALSE,
  output_path = NULL,
  treatment_var = "treatment",
  blocking_var = "blocking",
  daps,
  band_indices = c(B = 1, G = 2, R = 3, RE = 4, NIR = 5, T = 6)
)
}
\arguments{
\item{multi_path}{Character string. Path to the directory containing multispectral .tif files.}

\item{dsm_path}{Character string. Path to the directory containing DSM .tif files.}

\item{border_path}{Optional; Character string. Path to a single shapefile/geojson
used for all DAPs.}

\item{multiborder_path}{Optional; Character string. Path to a directory with
DAP-specific border files. Used if \code{border_path} is NULL.}

\item{dtm_file}{Character string. Path to the static DTM file.}

\item{save_rasters}{Logical. If TRUE, saves intermediate rasters (CC, CV, ExG, NDVI) to disk.}

\item{output_path}{Character string. Root directory where results and rasters will be saved.}

\item{treatment_var}{Character string. The column name in the polygon data
representing the treatment/unique ID. Default is "treatment".}

\item{blocking_var}{Character string. The column name for blocking/replication.
Default is "blocking".}

\item{daps}{A vector (numeric or character) of Days After Planting to process.}

\item{band_indices}{A named vector defining the band order in the multispectral files.
Default is \code{c(B=1, G=2, R=3, RE=4, NIR=5, T=6)}.}
}
\value{
A long-format data frame (tibble) containing extracted metrics (CV, ExG,
NDVI, CC) for every plot and every DAP processed.
}
\description{
This function automates the processing of multi-temporal remote sensing data.
For each provided Day After Planting (DAP), it aligns DSMs and DTMs, computes
Canopy Cover (CC), Canopy Volume (CV), and spectral indices (ExG, NDVI),
and performs zonal extraction based on provided plot boundaries.
}
\details{
The function expects files to follow a specific naming convention ending in
\code{_DAP.tif} (e.g., \code{imagery_45.tif}). It automatically handles spatial
alignment by resampling the DSM and DTM to match the multispectral resolution
and extent using bilinear interpolation.
}
