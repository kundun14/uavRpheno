% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pheno_functions.R
\name{extract_cv}
\alias{extract_cv}
\title{Extract Canopy Volume from DSM and DTM}
\usage{
extract_cv(dsm, dtm, polygons, id_col = "id", cc_mask = NULL)
}
\arguments{
\item{dsm}{A SpatRaster representing the Digital Surface Model (top of canopy).}

\item{dtm}{A SpatRaster representing the Digital Terrain Model (ground surface).}

\item{polygons}{An sf or SpatVector object defining the areas of interest.}

\item{id_col}{A character string for the unique identifier column in \code{polygons}.
Default is "id".}

\item{cc_mask}{An optional binary SpatRaster (1 = canopy, 0 = non-canopy).
If provided, heights in non-canopy areas are set to 0. Default is NULL.}
}
\value{
A data frame containing:
\itemize{
\item \code{COD}: The identifier from the polygon data.
\item \code{Volume_m3}: The total estimated canopy volume in cubic meters.
}
}
\description{
This function calculates the total canopy volume (m³) within specified polygons.
It generates a Canopy Height Model (CHM) by subtracting the DTM from the DSM,
applies a height threshold, masks by canopy cover (optional), and integrates
pixel area to compute volume.
}
\details{
The function filters out noise by setting heights below 0.05m to zero.
Volume is calculated as: \eqn{Volume = \sum (Height \times Pixel Area)}.
}
