% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pheno_functions.R
\name{extract_ndvi_mean}
\alias{extract_ndvi_mean}
\title{Extract Mean Normalized Difference Vegetation Index (NDVI)}
\usage{
extract_ndvi_mean(red, nir, cc_mask, polygons, id_col = "id")
}
\arguments{
\item{red}{A SpatRaster or RasterLayer representing the Red channel (usually ~660nm).}

\item{nir}{A SpatRaster or RasterLayer representing the Near-Infrared channel (usually ~800nm).}

\item{cc_mask}{A binary mask SpatRaster (1 = canopy, 0 = non-canopy) used
to restrict calculation to vegetation pixels.}

\item{polygons}{An sf or SpatVector object defining the areas (e.g., plots or tree crowns)
for extraction.}

\item{id_col}{A character string for the unique identifier column in \code{polygons}.
Default is "id".}
}
\value{
A data frame containing:
\itemize{
\item \code{COD}: The identifier from the polygon data.
\item \code{NDVI}: The mean NDVI value per polygon.
}
}
\description{
This function calculates the NDVI from Red and Near-Infrared (NIR) bands,
masks the result to canopy areas, and extracts the mean value within specified polygons.
}
\details{
The NDVI is calculated as:
\deqn{NDVI = \frac{NIR - Red}{NIR + Red}}
Values typically range from -1 to 1, where high positive values indicate
dense, healthy vegetation. By masking with \code{cc_mask}, the function
ensures the mean represents only the canopy pixels.
}
