% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pheno_functions.R
\name{extract_exg_mean}
\alias{extract_exg_mean}
\title{Extract Mean Excess Green Index (ExG)}
\usage{
extract_exg_mean(red, green, blue, cc_mask, polygons, id_col = "id")
}
\arguments{
\item{red}{A SpatRaster or RasterLayer representing the Red channel.}

\item{green}{A SpatRaster or RasterLayer representing the Green channel.}

\item{blue}{A SpatRaster or RasterLayer representing the Blue channel.}

\item{cc_mask}{A binary mask SpatRaster (1 = canopy, 0 = non-canopy) used
to restrict calculation to vegetation pixels.}

\item{polygons}{An sf or SpatVector object defining the areas for extraction.}

\item{id_col}{A character string for the unique identifier column in \code{polygons}.
Default is "id".}
}
\value{
A data frame containing:
\itemize{
\item \code{COD}: The identifier from the polygon data.
\item \code{ExG}: The mean Excess Green Index value per polygon.
}
}
\description{
This function calculates the Excess Green Index (ExG) from RGB bands,
masks it to canopy areas, and extracts the mean value within specified polygons.
}
\details{
The Excess Green Index is calculated using normalized RGB values:
\itemize{
\item \eqn{r = R / (R + G + B)}
\item \eqn{g = G / (R + G + B)}
\item \eqn{b = B / (R + G + B)}
\item \eqn{ExG = 2g - r - b}
}
This index is particularly effective for separating green vegetation from soil
and residue backgrounds.
}
